; Upload limits for product gallery batch uploads (Apache/Nginx php-fpm).
; For `php artisan serve`, also set these in php.ini or:
;   php -d max_file_uploads=100 -d post_max_size=128M -d upload_max_filesize=64M artisan serve
max_file_uploads = 100
post_max_size = 128M
upload_max_filesize = 64M
